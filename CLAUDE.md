# CLAUDE.md - Контекст для Claude Code

## О проекте

**Shopping List App** - простое веб-приложение для ведения списка покупок.

- **Стек:** Vanilla HTML5 + CSS3 + JavaScript (без фреймворков)
- **Язык интерфейса и комментариев:** Русский
- **Демо:** https://makarenko444.github.io/shopping-list-app/

## Архитектура

### Структура файлов

```
shopping-list-app/
├── index.html          # Главная страница
├── css/style.css       # Все стили
└── js/app.js           # Логика (класс ShoppingApp)
```

### Ключевые принципы

1. **Класс-ориентированная архитектура** - вся логика в классе `ShoppingApp`
2. **Реактивный рендеринг** - при изменении данных вызывается `render()`
3. **localStorage** - автосохранение при любых изменениях
4. **Глобальный экземпляр** - `window.app` для доступа из inline-обработчиков

### Структура данных товара

```javascript
{
    id: Number,           // Date.now()
    text: String,         // название товара
    completed: Boolean,   // куплен или нет
    createdAt: String     // дата (ru-RU формат)
}
```

## КРИТИЧНО: Безопасность кода

### ВСЕГДА проверять на OWASP Top 10

**Особо актуально для этого проекта:**

1. **XSS (Cross-Site Scripting)** ⚠️
   - Никогда не вставлять пользовательский ввод через `innerHTML` без санитизации
   - Использовать `textContent` для текста или экранировать HTML
   - Валидировать данные из localStorage

2. **localStorage Security**
   - Не хранить чувствительные данные
   - Валидировать данные после `JSON.parse()`

3. **Injection-атаки**
   - Проверять все входные данные
   - Не использовать `eval()` или `Function()` с пользовательским вводом

### ⚠️ Текущая уязвимость XSS в проекте

```javascript
// js/app.js:75-84 - УЯЗВИМОСТЬ!
li.innerHTML = `<span class="item-text">${item.text}</span>`;
// Нет экранирования! XSS возможен.
```

## Соглашения о коде

### JavaScript

1. **ES6+:** классы, стрелочные функции, const/let
2. **Комментарии на русском языке**
3. **camelCase** для методов и переменных
4. **Inline обработчики** в шаблонах: `onclick="app.deleteItem(${id})"`
5. **БЕЗОПАСНОСТЬ ПРЕВЫШЕ ВСЕГО** - проверка на OWASP Top 10

### CSS

- **Цветовая схема:** Градиент #667eea → #764ba2
- **Методология:** Простой CSS, Flexbox
- **Transitions:** 0.3s для плавности

### HTML

- **Язык:** `lang="ru"`
- **Семантичность:** ul, li, button
- **Эмодзи в UI:** 📝, 🗑️

## Git Workflow

### ОБЯЗАТЕЛЬНО после завершения любого блока работы:

1. Сделать коммит с эмодзи-префиксом
2. Запушить на GitHub

```bash
git add .
git commit -m "эмодзи Описание"
git push
```

**Примеры эмодзи:**
- ✨ Новая функция
- 🐛 Исправление бага
- 📝 Документация
- ⚡ Оптимизация
- 🎨 Стили/UI

### ВАЖНО: Пользователь плохо знает командную строку

**При работе с командами:**
1. Давать подробные пошаговые инструкции
2. Показывать полные команды с объяснениями
3. Указывать где выполнять (терминал VSCode, PowerShell)
4. Объяснять что делает каждая команда

**Правильно:**
```
Откройте терминал в VSCode (Ctrl + `) и выполните:

1. Проверить статус:
   git status
   (показывает измененные файлы)

2. Добавить файлы:
   git add .
   (точка = все файлы)
```

**Неправильно:**
```
Просто сделай git add и push
```

## Деплой

- **Main branch:** `main`
- **Деплой:** GitHub Pages (автоматически из main)
- **Репозиторий:** https://github.com/Makarenko444/shopping-list-app
