# CLAUDE.md - Контекст для Claude Code

## О проекте

**Shopping List App** - простое веб-приложение для ведения списка покупок с автоматическим сохранением в localStorage.

- **Демо:** https://makarenko444.github.io/shopping-list-app/
- **Язык:** Русский (интерфейс и комментарии)
- **Стек:** Vanilla HTML5 + CSS3 + JavaScript (без фреймворков)

## Архитектура проекта

### Структура файлов

```
shopping-list-app/
├── index.html          # Главная страница
├── css/
│   └── style.css       # Все стили приложения
├── js/
│   └── app.js          # Вся логика приложения (класс ShoppingApp)
└── src/
    └── app.js          # (дубликат, возможно для сборки)
```

### Архитектурные принципы

1. **Одностраничное приложение (SPA)** - весь UI управляется через JavaScript
2. **Класс-ориентированная архитектура** - основная логика в классе `ShoppingApp`
3. **Реактивный рендеринг** - при любом изменении данных вызывается `render()` для перерисовки UI
4. **Глобальный экземпляр** - приложение доступно через `window.app`

## Основные компоненты

### ShoppingApp (js/app.js)

**Основной класс приложения** с методами:

- `constructor()` - инициализация приложения
- `initElements()` - получение DOM-элементов
- `attachEvents()` - привязка обработчиков событий
- `addItem()` - добавление нового товара
- `toggleItem(id)` - переключение статуса (куплен/не куплен)
- `deleteItem(id)` - удаление товара
- `render()` - отрисовка всего списка
- `updateStats()` - обновление статистики
- `saveItems()` - сохранение в localStorage
- `loadItems()` - загрузка из localStorage

### Структура данных товара

```javascript
{
    id: Number,           // timestamp (Date.now())
    text: String,         // название товара
    completed: Boolean,   // статус (куплен или нет)
    createdAt: String     // дата создания (ru-RU формат)
}
```

### localStorage

- **Ключ:** `shoppingItems`
- **Формат:** JSON-массив объектов товаров
- **Автосохранение:** при любом изменении (add, toggle, delete)

## Стилистические особенности

### Дизайн

- **Цветовая схема:** Фиолетовый градиент (667eea → 764ba2)
- **Кнопка добавления:** #667eea
- **Кнопка удаления:** #ff4757
- **Стиль:** Modern, минималистичный, с мягкими тенями и скруглениями

### CSS-классы

- `.container` - основной контейнер (max-width: 500px)
- `.add-item` - блок добавления товара
- `.shopping-item` - элемент списка
- `.shopping-item.completed` - купленный товар (opacity: 0.6, line-through)

## Безопасность кода

### КРИТИЧЕСКИ ВАЖНО: Всегда проверяй код на безопасность!

При любых изменениях кода обязательно проверять на:

1. **XSS (Cross-Site Scripting)**
   - Никогда не вставлять пользовательский ввод напрямую через `innerHTML` без санитизации
   - Использовать `textContent` для текста или экранировать HTML-символы
   - Валидировать все данные из localStorage перед использованием

2. **Injection-атаки**
   - Проверять все входные данные
   - Не использовать `eval()` или `Function()` с пользовательским вводом
   - Осторожно с динамическим созданием HTML

3. **localStorage Security**
   - Не хранить чувствительные данные (пароли, токены)
   - Валидировать данные при загрузке из localStorage
   - Проверять типы данных после `JSON.parse()`

4. **OWASP Top 10**
   - SQL Injection (N/A - нет БД)
   - Broken Authentication (N/A - нет аутентификации)
   - Sensitive Data Exposure
   - XML External Entities (N/A)
   - Broken Access Control
   - Security Misconfiguration
   - XSS ✓ (актуально!)
   - Insecure Deserialization (проверять JSON из localStorage)
   - Using Components with Known Vulnerabilities
   - Insufficient Logging & Monitoring

### Текущие проблемы безопасности

⚠️ **ВНИМАНИЕ:** В текущей версии есть потенциальная уязвимость XSS:

```javascript
// js/app.js:75-84 - УЯЗВИМОСТЬ!
li.innerHTML = `
    <input type="checkbox" ...>
    <span class="item-text">${item.text}</span>  // <- Нет экранирования!
    <button class="delete-btn" onclick="app.deleteItem(${item.id})">
        🗑️
    </button>
`;
```

Если пользователь введёт `<script>alert('XSS')</script>`, код выполнится!

**Рекомендация:** Использовать экранирование HTML или создавать элементы через `createElement()`.

## Соглашения о коде

### JavaScript

1. **ES6+ синтаксис:** классы, стрелочные функции, const/let
2. **Комментарии на русском языке**
3. **Именование:** camelCase для методов и переменных
4. **Inline обработчики:** используются в шаблонах (`onclick="app.deleteItem(${id})"`)
5. **Глобальная функция:** `clearAllItems()` - доступна извне для очистки списка
6. **БЕЗОПАСНОСТЬ ПРЕВЫШЕ ВСЕГО:** Любой код должен проходить проверку на OWASP Top 10

### HTML

- **Язык:** `lang="ru"`
- **Семантичность:** используются правильные теги (ul, li, button)
- **Эмодзи в UI:** 📝 для заголовка, 🗑️ для кнопки удаления

### CSS

- **Методология:** Простой CSS без препроцессоров
- **Layout:** Flexbox для компоновки
- **Адаптивность:** Мобильно-дружественный дизайн
- **Transitions:** 0.3s для плавных переходов

## Функциональность

### Основные возможности

1. Добавление товара (кнопка или Enter)
2. Отметка товара как купленного (checkbox)
3. Удаление товара (кнопка с эмодзи корзины)
4. Статистика: всего товаров и сколько куплено
5. Автоматическое сохранение в localStorage

### Валидация

- Пустые строки не добавляются (показывается alert)
- Автоматический trim() при добавлении

## Особенности разработки

### При внесении изменений учитывайте:

1. **Русский язык** - весь UI, комментарии и сообщения на русском
2. **Простота кода** - проект учебный, избегать сложных паттернов
3. **Без зависимостей** - чистый JavaScript, никаких npm-пакетов
4. **Обратная совместимость** - данные в localStorage должны сохранять формат
5. **GitHub Pages** - изменения деплоятся автоматически в main ветку

### Типичные задачи

- **Добавить функцию:** Расширить класс ShoppingApp новым методом
- **Изменить UI:** Отредактировать index.html и style.css
- **Добавить поле:** Расширить структуру объекта товара, обновить render()
- **Фикс багов:** Проверить логику в методах класса ShoppingApp

## Git и деплой

- **Main branch:** `main`
- **Деплой:** GitHub Pages (автоматически из main)
- **Коммиты:** Используются эмодзи-префиксы (⚡, 🗑️, 🤖, ✨)

### ВАЖНО: Workflow после завершения работы

**По окончанию любого блока работы ОБЯЗАТЕЛЬНО:**

1. Сделать коммит с понятным сообщением
2. Запушить изменения в удалённый репозиторий

```bash
git add .
git commit -m "эмодзи Описание изменений"
git push
```

Примеры хороших коммитов:
- ✨ Добавлена функция очистки списка
- 🐛 Исправлена XSS-уязвимость в рендере
- 📝 Обновлена документация CLAUDE.md
- ⚡ Оптимизирован метод render()
- 🎨 Улучшен дизайн кнопок

## Известные ограничения

1. ID на основе `Date.now()` - может быть коллизия при быстром добавлении
2. `alert()` для валидации - можно улучшить UX
3. Inline обработчики событий - не лучшая практика, но работает
4. Нет категорий товаров
5. Нет сортировки/фильтрации
6. Нет экспорта/импорта списка

## Контакты

- **Автор:** Создано с помощью Claude AI
- **Репозиторий:** https://github.com/Makarenko444/shopping-list-app
