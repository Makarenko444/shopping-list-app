# 📦 Git - Шпаргалка для начинающих

## Что такое Git?

**Git** - это система контроля версий. Представьте что это "машина времени" для вашего кода:
- Сохраняет историю всех изменений
- Можно откатиться к любой версии
- Можно работать в команде без конфликтов

**GitHub** - это сайт где хранятся Git репозитории (как облако для кода).

---

## 🎯 Основные команды

### Проверка статуса
```bash
git status
```
Показывает что изменилось в проекте.

### Добавление файлов в коммит
```bash
# Добавить один файл
git add index.html

# Добавить все изменённые файлы
git add .

# Добавить все файлы определённого типа
git add *.js
```

### Создание коммита (сохранение изменений)
```bash
git commit -m "Добавил кнопку очистки списка"
```

**Правила хороших коммитов:**
- Пишите понятное сообщение
- Одно изменение = один коммит
- Используйте глаголы в прошедшем времени

**Примеры:**
- ✅ "Добавил тёмную тему"
- ✅ "Исправил ошибку с сохранением"
- ✅ "Обновил стили кнопок"
- ❌ "изменения"
- ❌ "фикс"
- ❌ "asdasd"

### Отправка на GitHub
```bash
git push
```

### Получение изменений с GitHub
```bash
git pull
```

---

## 🔄 Типичный рабочий процесс

```bash
# 1. Проверяем что изменилось
git status

# 2. Смотрим детали изменений
git diff

# 3. Добавляем файлы
git add .

# 4. Создаём коммит
git commit -m "Описание изменений"

# 5. Отправляем на GitHub
git push
```

---

## 🌿 Работа с ветками

### Что такое ветка?
Ветка - это отдельная "версия" проекта. Например:
- `main` - основная ветка (рабочая версия)
- `feature/dark-mode` - ветка для новой функции
- `bugfix/save-error` - ветка для исправления бага

### Команды для веток
```bash
# Посмотреть все ветки
git branch

# Создать новую ветку
git branch feature/new-button

# Переключиться на ветку
git checkout feature/new-button

# Создать и сразу переключиться (короче)
git checkout -b feature/new-button

# Удалить ветку
git branch -d feature/new-button
```

### Workflow с ветками
```bash
# 1. Создаём ветку для новой фичи
git checkout -b feature/categories

# 2. Работаем, делаем коммиты
git add .
git commit -m "Добавил категории товаров"

# 3. Отправляем ветку на GitHub
git push -u origin feature/categories

# 4. Переключаемся обратно на main
git checkout main

# 5. Сливаем изменения
git merge feature/categories
```

---

## 🔙 Откат изменений

### Отменить изменения в файле (не закоммичено)
```bash
git checkout -- index.html
```

### Убрать файл из staged (после git add)
```bash
git reset index.html
```

### Откатить последний коммит (но оставить изменения)
```bash
git reset --soft HEAD~1
```

### Откатить последний коммит (и удалить изменения) ⚠️
```bash
git reset --hard HEAD~1
```

---

## 📝 История коммитов

### Посмотреть историю
```bash
# Красивый лог
git log --oneline --graph --all

# Последние 5 коммитов
git log -5

# С датами
git log --pretty=format:"%h - %an, %ar : %s"
```

---

## 🆘 Частые проблемы

### Проблема: Забыл добавить файл в коммит
```bash
# Добавляем файл
git add забытый-файл.js

# Изменяем последний коммит
git commit --amend --no-edit
```

### Проблема: Ошибка в сообщении коммита
```bash
git commit --amend -m "Правильное сообщение"
```

### Проблема: Конфликт при pull
```bash
# 1. Git покажет файлы с конфликтами
git status

# 2. Откройте файл, найдите:
# <<<<<<< HEAD
# ваш код
# =======
# чужой код
# >>>>>>> origin/main

# 3. Исправьте вручную, оставив нужное

# 4. Добавьте и закоммитьте
git add .
git commit -m "Разрешил конфликт"
```

### Проблема: Случайно закоммитил секретный файл
```bash
# Удалить из Git но оставить на диске
git rm --cached secrets.txt
echo "secrets.txt" >> .gitignore
git commit -m "Убрал секретный файл"
```

---

## 🚫 .gitignore

Создайте файл `.gitignore` для игнорирования файлов:

```
# Node modules
node_modules/

# Логи
*.log

# Секретные файлы
.env
secrets.txt

# Системные файлы
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
```

---

## 📚 Полезные алиасы

Добавьте в `~/.gitconfig`:

```
[alias]
    st = status
    co = checkout
    ci = commit
    br = branch
    lg = log --oneline --graph --all
    last = log -1 HEAD
    unstage = reset HEAD --
```

Теперь можно писать короче:
```bash
git st        # вместо git status
git co main   # вместо git checkout main
git ci        # вместо git commit
```

---

## 🎓 Дополнительные команды

### Временно сохранить изменения
```bash
# Спрятать изменения
git stash

# Посмотреть что спрятано
git stash list

# Вернуть изменения
git stash pop
```

### Посмотреть кто менял строки
```bash
git blame index.html
```

### Поиск в истории
```bash
# Найти коммит с текстом
git log --all --grep="кнопка"

# Найти когда добавилась функция
git log -S "addItem"
```

---

## 💡 Советы

1. **Коммитьте часто** - маленькие коммиты лучше больших
2. **Пишите понятные сообщения** - через месяц забудете что делали
3. **Используйте ветки** - не бойтесь экспериментировать
4. **Делайте pull перед работой** - избежите конфликтов
5. **Не коммитьте всё подряд** - используйте .gitignore

---

## 🎯 Практика

### Задание 1: Первый коммит
```bash
# Измените что-нибудь в index.html
# Сделайте коммит
git add index.html
git commit -m "Изменил заголовок"
git push
```

### Задание 2: Работа с веткой
```bash
# Создайте ветку для эксперимента
git checkout -b experiment

# Поменяйте цвет кнопки в style.css
# Закоммитьте
git add .
git commit -m "Эксперимент с цветом"

# Вернитесь на main
git checkout main

# Посмотрите - цвет вернулся к старому!
```

### Задание 3: Откат изменений
```bash
# Сломайте что-нибудь в app.js
# Откатите изменения
git checkout -- js/app.js

# Файл восстановлен!
```

---

## 📖 Ресурсы для изучения

- **Интерактивный тур:** https://learngitbranching.js.org/
- **Шпаргалка:** https://training.github.com/downloads/ru/github-git-cheat-sheet/
- **Книга Pro Git (бесплатно):** https://git-scm.com/book/ru/v2
- **Визуализатор Git:** https://git-school.github.io/visualizing-git/

---

## 🎉 Заключение

Git может показаться сложным, но это мощный инструмент.

**Главное правило:** Если не уверены - создайте ветку и экспериментируйте там!

Всегда можно:
- Откатиться назад
- Создать новую ветку
- Попросить помощи

**Удачи! 🚀**
