# 📜 Скрипты для развертывания

Эта папка содержит скрипты для автоматизации развертывания проекта на VPS.

## 📦 Доступные скрипты

### 1. `deploy.sh` - Первоначальное развертывание

**Что делает:**
- Обновляет систему
- Устанавливает Nginx и Git
- Клонирует проект с GitHub
- Настраивает Nginx
- Создает скрипт автоматического обновления

**Использование:**

```bash
# На вашем сервере
cd /tmp
wget https://raw.githubusercontent.com/Makarenko444/shopping-list-app/main/scripts/deploy.sh
sudo bash deploy.sh
```

Или если проект уже склонирован:

```bash
cd /path/to/shopping-list-app/scripts
sudo bash deploy.sh
```

---

### 2. `update.sh` - Обновление проекта

**Что делает:**
- Получает последние изменения с GitHub
- Обновляет файлы проекта
- Настраивает права доступа

**Использование:**

```bash
sudo bash update.sh
```

Или из любого места:

```bash
sudo /usr/local/bin/update-shopping-list.sh
```

---

### 3. `setup-ssl.sh` - Установка SSL сертификата

**Что делает:**
- Устанавливает Certbot
- Получает бесплатный SSL сертификат от Let's Encrypt
- Настраивает автоматическое обновление

**Требования:**
- У вас должен быть домен
- Домен должен указывать на ваш сервер (A-запись)

**Использование:**

```bash
sudo bash setup-ssl.sh example.com
```

Замените `example.com` на ваш домен!

---

## 🚀 Быстрый старт

### Вариант 1: Полная автоматизация (рекомендуется)

Выполните на вашем VPS:

```bash
# 1. Скачать и запустить deploy.sh
wget https://raw.githubusercontent.com/Makarenko444/shopping-list-app/main/scripts/deploy.sh
sudo bash deploy.sh

# 2. После завершения - сайт доступен по IP!
# Откройте: http://ВАШ_IP

# 3. Если есть домен - настройте SSL
sudo bash /var/www/shopping-list/scripts/setup-ssl.sh example.com
```

### Вариант 2: Пошаговая установка

Следуйте инструкциям в **DEPLOYMENT-GUIDE.md**

---

## 🔄 Workflow разработки

### После внесения изменений в проект:

```bash
# На локальной машине
git add .
git commit -m "Описание изменений"
git push

# На сервере
sudo /usr/local/bin/update-shopping-list.sh
```

### С GitHub Webhooks (автоматически):

Настройте webhook (см. DEPLOYMENT-GUIDE.md) и просто делайте `git push` - сайт обновится автоматически!

---

## 🛠️ Дополнительные команды

### Посмотреть логи Nginx

```bash
# Последние ошибки
sudo tail -f /var/log/nginx/error.log

# Последние обращения
sudo tail -f /var/log/nginx/access.log
```

### Перезапустить Nginx

```bash
sudo systemctl reload nginx
```

### Проверить статус

```bash
sudo systemctl status nginx
```

---

## 🐛 Решение проблем

### Скрипт не запускается

```bash
# Сделайте файл исполняемым
chmod +x deploy.sh
```

### Ошибка "Permission denied"

```bash
# Запустите с sudo
sudo bash deploy.sh
```

### Сайт не работает после deploy.sh

```bash
# Проверьте логи
sudo nginx -t
sudo tail -f /var/log/nginx/error.log

# Проверьте что файлы на месте
ls -la /var/www/shopping-list
```

---

## 📝 Примечания

- Все скрипты должны запускаться с правами root (sudo)
- Скрипты протестированы на Ubuntu 18.04, 20.04, 22.04
- Для других дистрибутивов Linux могут потребоваться модификации

---

## 🎓 Что дальше?

После успешного развертывания:

1. ✅ Настройте домен и SSL
2. ✅ Настройте автоматическое обновление (webhook)
3. ✅ Добавьте мониторинг (UptimeRobot)
4. ✅ Настройте резервное копирование

---

*Подробные инструкции: [DEPLOYMENT-GUIDE.md](../DEPLOYMENT-GUIDE.md)*
