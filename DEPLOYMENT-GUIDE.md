# 🚀 Гайд по развертыванию на VPS (Ubuntu)

## 📚 Что вы узнаете

В этом руководстве мы:
- Подключимся к серверу по SSH
- Установим и настроим Nginx
- Склонируем проект с GitHub
- Настроим домен (опционально)
- Настроим SSL сертификат (бесплатный Let's Encrypt)
- Создадим скрипт автоматического обновления

**Время выполнения:** 30-60 минут

---

## ✅ Что вам понадобится

- [ ] IP адрес вашего VPS
- [ ] SSH доступ (логин/пароль или SSH ключ)
- [ ] Ubuntu сервер (16.04 или новее)
- [ ] Домен (опционально, но желательно)

---

## 🎯 Часть 1: Подключение к серверу

### Шаг 1: Подключение по SSH

```bash
# Из вашей локальной машины
ssh root@ВАШ_IP_АДРЕС

# Или если у вас отдельный пользователь
ssh username@ВАШ_IP_АДРЕС

# С указанием порта (если не стандартный 22)
ssh -p 2222 root@ВАШ_IP_АДРЕС
```

**Что происходит:**
- SSH (Secure Shell) - защищенное подключение к серверу
- `root` - пользователь с правами администратора
- После команды введите пароль

### Шаг 2: Обновление системы

```bash
# Обновить список пакетов
sudo apt update

# Установить обновления
sudo apt upgrade -y

# Проверить версию Ubuntu
lsb_release -a
```

**Объяснение:**
- `apt` - менеджер пакетов Ubuntu
- `update` - обновляет список доступных пакетов
- `upgrade` - устанавливает обновления
- `-y` - автоматически отвечает "да" на все вопросы

---

## 🌐 Часть 2: Установка Nginx

### Что такое Nginx?

**Nginx** - это веб-сервер. Он:
- Отдает файлы вашего сайта посетителям
- Быстрый и надежный
- Используется крупными компаниями (Netflix, Airbnb)

### Установка

```bash
# Установить Nginx
sudo apt install nginx -y

# Проверить статус
sudo systemctl status nginx

# Запустить Nginx (если не запущен)
sudo systemctl start nginx

# Добавить в автозагрузку
sudo systemctl enable nginx
```

### Проверка работы

Откройте в браузере: `http://ВАШ_IP_АДРЕС`

Должна появиться страница "Welcome to nginx!"

**Если не работает - проверьте файрвол:**
```bash
# Разрешить HTTP
sudo ufw allow 'Nginx HTTP'

# Разрешить HTTPS
sudo ufw allow 'Nginx HTTPS'

# Проверить статус
sudo ufw status
```

---

## 📦 Часть 3: Установка Git

```bash
# Установить Git
sudo apt install git -y

# Проверить версию
git --version

# Настроить Git
git config --global user.name "Ваше Имя"
git config --global user.email "your@email.com"
```

---

## 🔽 Часть 4: Клонирование проекта

### Шаг 1: Создание директории для сайта

```bash
# Перейти в корень веб-сервера
cd /var/www/

# Создать папку для вашего проекта
sudo mkdir -p shopping-list

# Перейти в папку
cd shopping-list
```

### Шаг 2: Клонирование репозитория

```bash
# Клонировать проект с GitHub
sudo git clone https://github.com/Makarenko444/shopping-list-app.git .

# Точка в конце означает "в текущую папку"

# Проверить что файлы скопировались
ls -la
```

Вы должны увидеть файлы: `index.html`, `css/`, `js/`, `README.md` и др.

### Шаг 3: Настройка прав доступа

```bash
# Дать Nginx права на чтение файлов
sudo chown -R www-data:www-data /var/www/shopping-list

# Установить правильные права
sudo chmod -R 755 /var/www/shopping-list
```

**Объяснение:**
- `www-data` - пользователь под которым работает Nginx
- `chown` - изменить владельца файлов
- `chmod` - изменить права доступа
- `755` - владелец может всё, остальные только читать

---

## ⚙️ Часть 5: Настройка Nginx

### Шаг 1: Создание конфигурации сайта

```bash
# Создать файл конфигурации
sudo nano /etc/nginx/sites-available/shopping-list
```

### Шаг 2: Добавьте конфигурацию

Вставьте следующий код:

```nginx
server {
    listen 80;
    listen [::]:80;

    # Замените на ваш домен или IP
    server_name ВАШ_IP_АДРЕС;
    # Если есть домен: server_name example.com www.example.com;

    root /var/www/shopping-list;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Кэширование статических файлов
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Gzip сжатие
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

**Сохранение:**
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter`
- Нажмите `Ctrl + X` (выйти)

### Шаг 3: Активация конфигурации

```bash
# Создать символическую ссылку
sudo ln -s /etc/nginx/sites-available/shopping-list /etc/nginx/sites-enabled/

# Проверить конфигурацию на ошибки
sudo nginx -t

# Если всё ОК, перезагрузить Nginx
sudo systemctl reload nginx
```

### Шаг 4: Проверка

Откройте в браузере: `http://ВАШ_IP_АДРЕС`

**Должно появиться ваше приложение Shopping List!** 🎉

---

## 🔒 Часть 6: Настройка HTTPS (SSL сертификат)

### Зачем нужен HTTPS?

- Шифрование данных
- Безопасность пользователей
- Google любит HTTPS сайты (лучше ранжирование)
- Современные браузеры требуют HTTPS для многих функций

### Установка Certbot (Let's Encrypt)

```bash
# Установить Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получить сертификат (замените на ваш домен!)
sudo certbot --nginx -d example.com -d www.example.com

# Или просто для IP (не рекомендуется, лучше с доменом)
# Let's Encrypt не выдает сертификаты для IP адресов!
```

**Следуйте инструкциям:**
1. Введите email
2. Согласитесь с условиями (Y)
3. Выберите редирект с HTTP на HTTPS (рекомендуется - 2)

### Автоматическое обновление сертификата

```bash
# Certbot автоматически добавляет задачу в cron
# Проверим что она есть
sudo systemctl status certbot.timer

# Тестовое обновление (не обновит реально)
sudo certbot renew --dry-run
```

Сертификат будет автоматически обновляться каждые 60 дней!

### Проверка

Откройте: `https://ВАШ_ДОМЕН`

Должен появиться замочек в адресной строке! 🔒

---

## 🔄 Часть 7: Автоматическое обновление с GitHub

### Способ 1: Простой скрипт обновления

Создайте скрипт для обновления:

```bash
# Создать скрипт
sudo nano /usr/local/bin/update-shopping-list.sh
```

Вставьте:

```bash
#!/bin/bash

echo "🔄 Обновление Shopping List App..."

# Перейти в директорию проекта
cd /var/www/shopping-list

# Сохранить изменения (если есть)
git stash

# Получить последние изменения
git pull origin main

# Восстановить изменения
git stash pop 2>/dev/null || true

# Установить права
chown -R www-data:www-data /var/www/shopping-list
chmod -R 755 /var/www/shopping-list

echo "✅ Обновление завершено!"
echo "🌐 Проверьте сайт: http://$(hostname -I | cut -d' ' -f1)"
```

Сделайте скрипт исполняемым:

```bash
sudo chmod +x /usr/local/bin/update-shopping-list.sh
```

### Использование скрипта

```bash
# Запустить обновление
sudo /usr/local/bin/update-shopping-list.sh
```

### Способ 2: GitHub Webhooks (продвинутый)

Автоматическое обновление при каждом `git push`!

**Создадим простой сервер для webhooks:**

```bash
# Установить Node.js (если нет)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Создать директорию
sudo mkdir -p /var/www/webhook
cd /var/www/webhook

# Создать файл webhook сервера
sudo nano webhook.js
```

Вставьте код:

```javascript
const http = require('http');
const { exec } = require('child_process');

const PORT = 9000;
const SECRET = 'YOUR_SECRET_KEY'; // Измените на свой секрет!

http.createServer((req, res) => {
    if (req.method === 'POST' && req.url === '/webhook') {
        let body = '';

        req.on('data', chunk => {
            body += chunk.toString();
        });

        req.on('end', () => {
            console.log('📥 Webhook получен');

            // Запускаем скрипт обновления
            exec('/usr/local/bin/update-shopping-list.sh', (error, stdout, stderr) => {
                if (error) {
                    console.error('❌ Ошибка:', error);
                    return;
                }
                console.log('✅ Обновлено:', stdout);
            });

            res.writeHead(200);
            res.end('OK');
        });
    } else {
        res.writeHead(404);
        res.end('Not Found');
    }
}).listen(PORT, () => {
    console.log(`🎣 Webhook сервер запущен на порту ${PORT}`);
});
```

Создайте systemd сервис:

```bash
sudo nano /etc/systemd/system/webhook.service
```

Вставьте:

```ini
[Unit]
Description=GitHub Webhook Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/www/webhook
ExecStart=/usr/bin/node /var/www/webhook/webhook.js
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl start webhook
sudo systemctl enable webhook
sudo systemctl status webhook
```

Откройте порт:

```bash
sudo ufw allow 9000/tcp
```

**Настройка в GitHub:**

1. Откройте ваш репозиторий на GitHub
2. Settings → Webhooks → Add webhook
3. Payload URL: `http://ВАШ_IP:9000/webhook`
4. Content type: `application/json`
5. Secret: `YOUR_SECRET_KEY` (тот же что в коде)
6. Events: Just the `push` event
7. Active: ✓
8. Add webhook

Теперь при каждом `git push` сайт будет автоматически обновляться! 🚀

---

## 🛠️ Полезные команды

### Управление Nginx

```bash
# Запустить
sudo systemctl start nginx

# Остановить
sudo systemctl stop nginx

# Перезапустить
sudo systemctl restart nginx

# Перезагрузить конфигурацию (без даунтайма)
sudo systemctl reload nginx

# Проверить статус
sudo systemctl status nginx

# Проверить конфигурацию
sudo nginx -t

# Посмотреть логи ошибок
sudo tail -f /var/log/nginx/error.log

# Посмотреть логи доступа
sudo tail -f /var/log/nginx/access.log
```

### Управление Git на сервере

```bash
# Обновить код
cd /var/www/shopping-list
sudo git pull

# Посмотреть статус
sudo git status

# Посмотреть изменения
sudo git log --oneline -5

# Откатить к определенному коммиту
sudo git reset --hard COMMIT_HASH
```

---

## 🐛 Решение проблем

### Проблема: 403 Forbidden

```bash
# Проверить права
ls -la /var/www/shopping-list

# Исправить права
sudo chown -R www-data:www-data /var/www/shopping-list
sudo chmod -R 755 /var/www/shopping-list
```

### Проблема: 404 Not Found

```bash
# Проверить путь к файлам в конфигурации
sudo nano /etc/nginx/sites-available/shopping-list

# Проверить что файлы на месте
ls -la /var/www/shopping-list
```

### Проблема: Nginx не запускается

```bash
# Посмотреть ошибки
sudo nginx -t
sudo systemctl status nginx

# Посмотреть логи
sudo tail -f /var/log/nginx/error.log
```

### Проблема: Сайт не обновляется

```bash
# Очистить кэш браузера (Ctrl + F5)

# Перезапустить Nginx
sudo systemctl reload nginx

# Проверить что код обновился
cd /var/www/shopping-list
git log -1
```

---

## 📊 Мониторинг сайта

### Просмотр логов в реальном времени

```bash
# Логи доступа (кто заходит)
sudo tail -f /var/log/nginx/access.log

# Логи ошибок
sudo tail -f /var/log/nginx/error.log
```

### Проверка использования ресурсов

```bash
# Использование диска
df -h

# Использование памяти
free -h

# Процессы
top

# Или более красивый вариант
htop  # (нужно установить: sudo apt install htop)
```

---

## 🎯 Чек-лист развертывания

- [ ] Подключился к серверу по SSH
- [ ] Обновил систему (`apt update && apt upgrade`)
- [ ] Установил Nginx
- [ ] Установил Git
- [ ] Склонировал проект в `/var/www/shopping-list`
- [ ] Настроил права доступа
- [ ] Создал конфигурацию Nginx
- [ ] Активировал конфигурацию
- [ ] Проверил что сайт работает по HTTP
- [ ] Настроил домен (опционально)
- [ ] Установил SSL сертификат (если есть домен)
- [ ] Создал скрипт автоматического обновления
- [ ] Настроил webhook (опционально)

---

## 🚀 Следующие шаги

### Безопасность

1. **Отключите вход по паролю, используйте SSH ключи**
2. **Настройте файрвол (UFW)**
3. **Установите Fail2Ban** (защита от брутфорса)
4. **Регулярно обновляйте систему**

### Оптимизация

1. **Настройте кэширование в Nginx**
2. **Включите Gzip сжатие** (уже в конфиге)
3. **Используйте CDN** (Cloudflare - бесплатно)

### Мониторинг

1. **UptimeRobot** - проверка доступности сайта
2. **Google Analytics** - аналитика посещений
3. **Настройте оповещения о падении сервера**

---

## 📚 Дополнительные материалы

### Полезные ссылки

- **Nginx документация:** https://nginx.org/ru/docs/
- **Certbot:** https://certbot.eff.org/
- **DigitalOcean туториалы:** https://www.digitalocean.com/community/tutorials
- **Ubuntu Server Guide:** https://ubuntu.com/server/docs

### Рекомендуемые инструменты

- **SSH клиенты:** PuTTY (Windows), Terminal (Mac/Linux)
- **SFTP клиенты:** FileZilla, WinSCP
- **Мониторинг:** Netdata, Grafana

---

## 🎉 Поздравляю!

Вы развернули свой первый проект на настоящем сервере!

Теперь вы знаете:
- ✅ Как работать с Linux сервером
- ✅ Что такое Nginx и как его настраивать
- ✅ Как клонировать проект с GitHub
- ✅ Как настроить HTTPS
- ✅ Как автоматизировать обновления

**Это серьёзный скилл который ценится работодателями!** 🚀

---

*Если что-то непонятно - спрашивайте! Удачи в развертывании!* 💪
