# ✅ Чек-лист развертывания на VPS

Используйте этот чек-лист для проверки каждого шага развертывания.

---

## 🎯 Этап 1: Подготовка

### Информация о сервере

Соберите следующую информацию перед началом:

- [ ] IP адрес сервера: `___________________`
- [ ] SSH порт (обычно 22): `___________________`
- [ ] Имя пользователя: `___________________`
- [ ] Пароль или SSH ключ: `[сохранено]`
- [ ] Домен (опционально): `___________________`

### Проверка доступа

- [ ] Могу подключиться по SSH
  ```bash
  ssh username@IP_ADDRESS
  ```

- [ ] Пользователь имеет права sudo
  ```bash
  sudo whoami  # Должно вывести: root
  ```

- [ ] Проверил версию Ubuntu
  ```bash
  lsb_release -a
  ```
  Версия: `___________________`

---

## 🔧 Этап 2: Установка необходимого ПО

### Обновление системы

- [ ] Обновил список пакетов
  ```bash
  sudo apt update
  ```

- [ ] Установил обновления
  ```bash
  sudo apt upgrade -y
  ```

- [ ] Перезагрузил сервер (если требуется)
  ```bash
  sudo reboot
  ```

### Установка Nginx

- [ ] Установил Nginx
  ```bash
  sudo apt install nginx -y
  ```

- [ ] Проверил что Nginx запущен
  ```bash
  sudo systemctl status nginx
  ```

- [ ] Добавил Nginx в автозагрузку
  ```bash
  sudo systemctl enable nginx
  ```

- [ ] Проверил доступность по IP
  - Открыл в браузере: `http://ВАШ_IP`
  - Увидел страницу "Welcome to nginx!"

### Установка Git

- [ ] Установил Git
  ```bash
  sudo apt install git -y
  ```

- [ ] Проверил версию
  ```bash
  git --version
  ```
  Версия: `___________________`

- [ ] Настроил Git
  ```bash
  git config --global user.name "Ваше Имя"
  git config --global user.email "your@email.com"
  ```

---

## 📦 Этап 3: Развертывание проекта

### Создание структуры

- [ ] Создал директорию для сайта
  ```bash
  sudo mkdir -p /var/www/shopping-list
  ```

- [ ] Перешел в директорию
  ```bash
  cd /var/www/shopping-list
  ```

### Клонирование проекта

- [ ] Склонировал репозиторий
  ```bash
  sudo git clone https://github.com/Makarenko444/shopping-list-app.git .
  ```

- [ ] Проверил что файлы скопировались
  ```bash
  ls -la
  ```
  Вижу файлы: `index.html`, `css/`, `js/`, `README.md`

### Права доступа

- [ ] Установил владельца
  ```bash
  sudo chown -R www-data:www-data /var/www/shopping-list
  ```

- [ ] Установил права
  ```bash
  sudo chmod -R 755 /var/www/shopping-list
  ```

- [ ] Проверил права
  ```bash
  ls -la /var/www/shopping-list
  ```

---

## ⚙️ Этап 4: Настройка Nginx

### Создание конфигурации

- [ ] Создал конфигурационный файл
  ```bash
  sudo nano /etc/nginx/sites-available/shopping-list
  ```

- [ ] Вставил конфигурацию (см. DEPLOYMENT-GUIDE.md)

- [ ] Указал правильный server_name (IP или домен)

- [ ] Сохранил файл (Ctrl+O, Enter, Ctrl+X)

### Активация конфигурации

- [ ] Создал символическую ссылку
  ```bash
  sudo ln -s /etc/nginx/sites-available/shopping-list /etc/nginx/sites-enabled/
  ```

- [ ] Проверил конфигурацию на ошибки
  ```bash
  sudo nginx -t
  ```
  Вывод: `syntax is ok` и `test is successful`

- [ ] Перезагрузил Nginx
  ```bash
  sudo systemctl reload nginx
  ```

### Проверка работы

- [ ] Открыл сайт в браузере: `http://ВАШ_IP`
- [ ] Вижу приложение Shopping List
- [ ] Могу добавить товар
- [ ] Могу отметить товар как купленный
- [ ] Могу удалить товар
- [ ] Данные сохраняются (localStorage работает)

---

## 🔥 Этап 5: Настройка файрвола

### UFW (Uncomplicated Firewall)

- [ ] Проверил статус UFW
  ```bash
  sudo ufw status
  ```

- [ ] Разрешил SSH (ВАЖНО! Иначе потеряете доступ)
  ```bash
  sudo ufw allow OpenSSH
  ```

- [ ] Разрешил HTTP
  ```bash
  sudo ufw allow 'Nginx HTTP'
  ```

- [ ] Разрешил HTTPS
  ```bash
  sudo ufw allow 'Nginx HTTPS'
  ```

- [ ] Включил UFW (если выключен)
  ```bash
  sudo ufw enable
  ```

- [ ] Проверил правила
  ```bash
  sudo ufw status verbose
  ```

---

## 🔄 Этап 6: Автоматизация обновлений

### Скрипт обновления

- [ ] Создал скрипт обновления
  ```bash
  sudo nano /usr/local/bin/update-shopping-list.sh
  ```

- [ ] Вставил код скрипта (см. DEPLOYMENT-GUIDE.md)

- [ ] Сделал скрипт исполняемым
  ```bash
  sudo chmod +x /usr/local/bin/update-shopping-list.sh
  ```

- [ ] Проверил работу скрипта
  ```bash
  sudo /usr/local/bin/update-shopping-list.sh
  ```

---

## 🔒 Этап 7: HTTPS (опционально, но рекомендуется)

**Требования:** У вас должен быть домен, указывающий на сервер

### Настройка домена

- [ ] Создал A-запись домена на IP сервера
  - Домен: `___________________`
  - IP: `___________________`

- [ ] Проверил что домен работает
  ```bash
  ping example.com
  ```

- [ ] Обновил server_name в Nginx на домен
  ```bash
  sudo nano /etc/nginx/sites-available/shopping-list
  ```

- [ ] Перезагрузил Nginx
  ```bash
  sudo nginx -t && sudo systemctl reload nginx
  ```

### Установка SSL

- [ ] Установил Certbot
  ```bash
  sudo apt install certbot python3-certbot-nginx -y
  ```

- [ ] Получил сертификат
  ```bash
  sudo certbot --nginx -d example.com -d www.example.com
  ```

- [ ] Ввел email: `___________________`

- [ ] Согласился с условиями

- [ ] Выбрал редирект на HTTPS (опция 2)

- [ ] Проверил автообновление
  ```bash
  sudo systemctl status certbot.timer
  ```

- [ ] Протестировал обновление
  ```bash
  sudo certbot renew --dry-run
  ```

### Проверка HTTPS

- [ ] Открыл сайт: `https://example.com`
- [ ] Вижу замочек в адресной строке
- [ ] Сертификат валиден
- [ ] HTTP редиректит на HTTPS

---

## 🎣 Этап 8: GitHub Webhooks (опционально)

### Установка Node.js

- [ ] Установил Node.js
  ```bash
  curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
  sudo apt install -y nodejs
  ```

- [ ] Проверил версию
  ```bash
  node --version
  ```

### Настройка webhook сервера

- [ ] Создал директорию
  ```bash
  sudo mkdir -p /var/www/webhook
  ```

- [ ] Создал файл webhook.js (см. DEPLOYMENT-GUIDE.md)

- [ ] Создал systemd сервис

- [ ] Запустил сервис
  ```bash
  sudo systemctl start webhook
  sudo systemctl enable webhook
  ```

- [ ] Проверил статус
  ```bash
  sudo systemctl status webhook
  ```

- [ ] Открыл порт 9000
  ```bash
  sudo ufw allow 9000/tcp
  ```

### Настройка в GitHub

- [ ] Открыл репозиторий на GitHub
- [ ] Settings → Webhooks → Add webhook
- [ ] Указал Payload URL: `http://IP:9000/webhook`
- [ ] Выбрал Content type: `application/json`
- [ ] Указал Secret (такой же как в коде)
- [ ] Выбрал событие: Just the push event
- [ ] Активировал webhook
- [ ] Протестировал (сделал commit и push)

---

## 🧪 Этап 9: Тестирование

### Функциональность

- [ ] Могу добавить товар
- [ ] Могу отметить товар купленным
- [ ] Могу удалить товар
- [ ] Счётчик товаров обновляется
- [ ] Данные сохраняются после перезагрузки страницы

### Производительность

- [ ] Сайт загружается быстро (< 2 секунд)
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] Нет ошибок в логах Nginx
  ```bash
  sudo tail -f /var/log/nginx/error.log
  ```

### Мобильная версия

- [ ] Проверил на телефоне
- [ ] Дизайн адаптивный
- [ ] Все функции работают

---

## 📊 Этап 10: Мониторинг

### Логи

- [ ] Знаю как смотреть логи доступа
  ```bash
  sudo tail -f /var/log/nginx/access.log
  ```

- [ ] Знаю как смотреть логи ошибок
  ```bash
  sudo tail -f /var/log/nginx/error.log
  ```

### Статус сервисов

- [ ] Nginx запущен
  ```bash
  sudo systemctl status nginx
  ```

- [ ] Webhook запущен (если настроен)
  ```bash
  sudo systemctl status webhook
  ```

### Uptime мониторинг (рекомендуется)

- [ ] Зарегистрировался на UptimeRobot.com
- [ ] Добавил мониторинг сайта
- [ ] Настроил email уведомления

---

## 🔐 Этап 11: Безопасность

### SSH

- [ ] Изменил SSH порт (опционально)
- [ ] Отключил вход root по паролю (рекомендуется)
- [ ] Настроил вход по SSH ключу

### Обновления

- [ ] Настроил автоматические обновления безопасности
  ```bash
  sudo apt install unattended-upgrades
  sudo dpkg-reconfigure --priority=low unattended-upgrades
  ```

### Fail2Ban (защита от брутфорса)

- [ ] Установил Fail2Ban
  ```bash
  sudo apt install fail2ban -y
  ```

- [ ] Запустил и добавил в автозагрузку
  ```bash
  sudo systemctl start fail2ban
  sudo systemctl enable fail2ban
  ```

---

## 📝 Этап 12: Документация

### Информация для себя

Сохраните эту информацию в надежном месте:

```
=== ИНФОРМАЦИЯ О СЕРВЕРЕ ===

IP адрес: ___________________
SSH порт: ___________________
Пользователь: ___________________
Домен: ___________________

=== ВАЖНЫЕ КОМАНДЫ ===

Обновить сайт:
sudo /usr/local/bin/update-shopping-list.sh

Перезапустить Nginx:
sudo systemctl reload nginx

Посмотреть логи:
sudo tail -f /var/log/nginx/error.log

=== ВАЖНЫЕ ФАЙЛЫ ===

Конфигурация Nginx:
/etc/nginx/sites-available/shopping-list

Файлы сайта:
/var/www/shopping-list

Скрипт обновления:
/usr/local/bin/update-shopping-list.sh

=== КОНТАКТЫ ===

Провайдер VPS: ___________________
Email для сертификата: ___________________
Дата установки: ___________________
```

---

## 🎉 Финальная проверка

### Всё работает?

- [ ] ✅ Сайт доступен по HTTP (или HTTPS)
- [ ] ✅ Все функции приложения работают
- [ ] ✅ Нет ошибок в логах
- [ ] ✅ Файрвол настроен
- [ ] ✅ SSL сертификат установлен (если есть домен)
- [ ] ✅ Автообновление настроено
- [ ] ✅ Мониторинг настроен
- [ ] ✅ Резервная копия информации создана

---

## 🚀 Следующие шаги

После успешного развертывания:

1. **Добавьте в закладки:**
   - Админ панель сервера (если есть)
   - Мониторинг (UptimeRobot)
   - Ваш сайт

2. **Регулярное обслуживание:**
   - Проверяйте логи раз в неделю
   - Обновляйте систему ежемесячно
   - Делайте резервные копии

3. **Улучшения:**
   - Добавьте Google Analytics
   - Настройте CDN (Cloudflare)
   - Оптимизируйте производительность

---

## 📚 Справка

При проблемах смотрите:
- **DEPLOYMENT-GUIDE.md** - подробная инструкция
- **scripts/README.md** - документация по скриптам
- Логи Nginx: `/var/log/nginx/`

---

**Поздравляю! Вы развернули свой первый проект на VPS! 🎉**

*Дата завершения: _________________*
